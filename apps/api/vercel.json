{
  "version": 2,
  "installCommand": "pnpm install && pnpm prisma generate",
  "buildCommand": "pnpm turbo run build --filter=@pdpx/api",
  "builds": [{ "src": "api/index.ts", "use": "@vercel/node@3" }],
  "routes": [{ "src": "/(.*)", "dest": "api/index.ts" }]
}
